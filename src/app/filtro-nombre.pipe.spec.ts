import { FiltroNombrePipe } from './filtro-nombre.pipe';

describe('FiltroNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
